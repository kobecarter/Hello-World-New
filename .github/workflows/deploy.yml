name: Deploy to production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: [self-hosted, macOS, deploy]
    steps:
      - name: Pull latest main on the server
        run: ssh hw-cpanel-deploy "cd ~/public_html/helloworld-agency.com/new/ && git pull origin main"
